export const TAGS = ["mutation", "trust", "state", "destructive"] as const;
export const CATEGORY = "mutation";
export const READ_ONLY = false;
export const PERMISSION_LEVEL = "write_local" as const;
